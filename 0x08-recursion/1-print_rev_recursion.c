#include "main.h"

/**
 * _print_rev_recursion - Prints string in reverse
 * @s: The string
 * Return: Reversed string
 */
int count(char s);

void _print_rev_recursion(char *s)
{
	int i = sizeof(s) - 1;

	if (i < 0)
		_putchar('\n'');

}

int count(char s)
{
	
