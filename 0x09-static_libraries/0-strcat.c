#include "main.h"
#include "string.h"

/**
 * _strcat - Join two strings together
 * @dest: First string
 * @src: Second string
 * Return: the result
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
