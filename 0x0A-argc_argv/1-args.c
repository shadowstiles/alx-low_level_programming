#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: argc
 * @argv: argv
 * Return: 0
 */
int argc;

int main()
{
	int i;
	int ans;

	for (i = 0; i < argc; i++)
	{
		ans = i;
	}

	printf("%d\n", ans);

	return (0);
}
