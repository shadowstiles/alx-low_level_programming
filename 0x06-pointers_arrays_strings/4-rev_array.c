#include "main.h"
#include <stdio.h>

/**
 * reverse_array - Revesre a string
 * @n: size of string
 * @a: string to reverse
 * Return: NULL
 */

void reverse_array(int *a, int n)
{
	int i = n - 1;

	while (i >= 0)
	{
		printf("%d", (a[i]);
		i--;
	}
}
